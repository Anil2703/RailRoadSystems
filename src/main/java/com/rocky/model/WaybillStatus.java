package com.rocky.model;

public enum WaybillStatus {
    PENDING,
    IN_TRANSIT,
    DELIVERED,
    CANCELLED,
    RETURNED,
    EXCEPTION;

    // You can add additional methods or fields if needed

}
